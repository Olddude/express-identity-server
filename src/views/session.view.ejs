<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/bootstrap.min.css">
  <link rel="stylesheet" href="/styles.css">
  <title>Sessions</title>
</head>
<body class="container-fluid">
  <div class="session">
    <h1>Sessions</h1>
    <ul class="list-group session__sessions">
      <% for (let i = 0; i < sessions.length; i++) { %>
        <li class="list-group-item session__sessions-item">
          <div class="row">
            <div class="col-xs-12"><%= sessions[i].expires %></div>
            <div class="col-xs-12"><%= sessions[i].session %></div>
          </div>
        </li>
      <% } %>
    </ul>
  </div>
</body>
</html>
